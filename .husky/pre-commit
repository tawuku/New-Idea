#!/usr/bin/env sh

# Run lint-staged (biome check + format on staged files)
npx lint-staged

# Scan for secrets
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact --no-banner
fi
